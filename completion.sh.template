#!/bin/bash
#This file was generated by small-script-repo and adds bash completion to your script repo

function _#NAME#_complete()
{
    cur=${COMP_WORDS[COMP_CWORD]}
    COMP_WORDS[0]="--list"
    current_cmd_list=$(#NAME# ${COMP_WORDS[*]})
    COMPREPLY=( $( compgen -W "${current_cmd_list}" -- "${cur}"  ) )
}

complete -F _#NAME#_complete #NAME#
